name: Lighthouse
on:
  push:
    branches: [source]
  pull_request:
    branches: [source]
permissions:
  contents: read
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        device: [mobile, desktop]
        group: [content, commercial]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      # Local production-mode output: preview noindex is deliberately preserved on Vercel.
      - run: npm run build && npm run check
      - run: npx --yes @lhci/cli@0.15.1 autorun
        env:
          LHCI_DEVICE: ${{ matrix.device }}
          LHCI_GROUP: ${{ matrix.group }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: lighthouse-${{ matrix.device }}-${{ matrix.group }}
          path: |
            lighthouse-reports
            .lighthouseci
