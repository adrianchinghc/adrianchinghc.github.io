---
layout: layouts/base.njk
title: Ideas
seoTitle: AI, software and growth ideas for founders
description: Videos and writing for founders deciding what to build, what to buy and what to leave alone. From Adrian Ching, founder of Second Team.
permalink: /blog/
socialImage: /assets/images/adrian/studio.webp
socialImageAlt: Adrian Ching recording a video
---
{% from "icons.njk" import icon %}
<header class="writing-hero ideas-introduction"><div class="shell">
  <h1>My videos and writing.</h1>
  <p class="page-lede">Videos and writing on AI, software and building a business. Pick the question closest to the decision you’re making.</p>
  <nav class="page-jumps" aria-label="Browse ideas"><a href="#featured">Choosing tools</a><a href="#popular">AI &amp; hiring</a><a href="#writing">Writing</a></nav>
</div></header>
<section class="section idea-collection" id="featured"><div class="shell">
  {% for video in media.featuredVideos %}
  <article class="idea-entry"{% if loop.index == 2 %} id="popular"{% endif %}>
    <a class="video-card idea-image" href="{{ video.url }}" target="_blank" rel="noopener" data-analytics-event="youtube_click" data-cta="ideas_video_image" data-video-title="{{ video.title }}" aria-label="Watch {{ video.title }} on YouTube (opens in a new tab)"><div class="video-thumbnail"><img src="{{ video.image }}" alt="" width="{{ video.width }}" height="{{ video.height }}" loading="lazy" decoding="async"><span class="video-play" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M8 5v14l11-7z"/></svg></span></div></a>
    <div class="idea-content"><p class="content-topic">{{ video.topic }}</p><h2>{{ video.question }}</h2><p>{{ video.context }}</p><p class="published-title">{{ video.title }}</p><div class="idea-action"><a class="text-link" href="{{ video.url }}" target="_blank" rel="noopener" data-analytics-event="youtube_click" data-cta="ideas_video_text" data-video-title="{{ video.title }}">Watch on YouTube<span class="sr-only">: {{ video.title }} (opens in a new tab)</span>{{ icon("external", "link-arrow") }}</a><span class="content-meta">{{ video.views }}</span></div></div>
  </article>
  {% endfor %}
  <p class="collection-note">A curated selection. View counts checked September 2026. Covers are editorial photos of me.</p>
</div></section>
<section class="section section-soft" id="latest"><div class="shell latest-note latest-with-photo"><figure class="recording-photo"><img src="/assets/images/adrian/behind-the-scenes.webp" alt="Behind the scenes as Adrian Ching records a conversation" width="900" height="1600" loading="lazy" decoding="async"><figcaption>Behind the scenes.</figcaption></figure><div>{{ icon("play", "section-icon") }}<h2>Keep up with the work.</h2><p>New videos go on YouTube first. You’ll find my latest uploads there.</p><a class="text-link" href="{{ media.youtube.channelUrl }}" target="_blank" rel="noopener" data-media-link="youtube-channel">Latest videos on YouTube{{ icon("external", "link-arrow") }}</a></div><div>{{ icon("mail", "section-icon") }}<h2>Prefer to read?</h2><p>Notes on building Second Team, choosing what to work on and the lessons I’m taking forward.</p><a class="text-link" href="/newsletter/">{% if site.newsletterUrl %}Join the newsletter{% else %}About the newsletter{% endif %}{{ icon("arrow", "link-arrow") }}</a></div></div></section>
<section class="section" id="writing"><div class="shell">
{% if collections.articles.length %}
<div class="section-heading-grid"><h2>Latest writing.</h2><p class="section-lede">Decisions, practical guides and lessons from building businesses.</p></div>
<div class="writing-list">{% for item in collections.articles %}
<a class="writing-entry" href="{{ item.url }}"><div class="article-byline"><span>{{ item.data.topic }}</span><time datetime="{{ item.data.date | isoDate }}">{{ item.data.date | readableDate }}</time></div><h3>{{ item.data.title }}</h3><p>{{ item.data.description }}</p><span class="text-link">Read the article</span></a>
{% endfor %}</div>
{% endif %}
<div class="section-heading-grid"><h2>Inside an audit.</h2><p class="section-lede">An illustrative review of missed B2B follow-ups, with possible priorities and a 90-day plan.</p></div><a class="archive-entry" href="/ai-profit-opportunity-audit/example/"><span class="content-meta">Illustrative example</span><h3>What an AI opportunity audit could look like</h3><span class="text-link">Read the example</span></a></div></section>
{% if collections.archiveArticles.length %}
<section class="section"><div class="shell"><div class="section-heading-grid"><h2>From the writing archive.</h2><p class="section-lede">Earlier writing, kept here with its original dates. Tools and instructions may be out of date.</p></div>
{% for item in collections.archiveArticles %}<a class="archive-entry" href="{{ item.url }}"><time datetime="{{ item.data.date | isoDate }}">{{ item.data.date | readableDate }}</time><h3>{{ item.data.title }}</h3><span class="text-link">Read the archive article</span></a>{% endfor %}
</div></section>
{% endif %}
