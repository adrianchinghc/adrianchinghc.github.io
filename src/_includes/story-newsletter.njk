{% from "icons.njk" import icon %}
<section class="newsletter-band"><div class="shell newsletter-grid"><div>{{ icon("mail", "section-icon") }}<h2>Build more with AI. Automate the busywork.</h2></div><div><p>Get practical ideas for using AI and automation, plus a look inside the businesses I’m building. I share the tools I’m trying, the decisions I’m making and the lessons you can take into your own work.</p>{% if site.newsletterUrl %}{% set signupPlacement = (page.url | replace("/", "") or "home") + "_band" %}{% include "newsletter-signup.njk" %}{% else %}<a class="text-link" href="/newsletter/">About the newsletter{{ icon("arrow", "link-arrow") }}</a>{% endif %}</div></div></section>
